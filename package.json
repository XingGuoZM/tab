{
  "name": "root",
  "private": true,
  "scripts": {
    "init": "lerna clean && lerna bootstrap",
    "start": "lerna run --scope examples start",
    "build": "lerna run --scope examples build"
  },
  "dependencies": {
    "better-scroll": "^2.2.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}